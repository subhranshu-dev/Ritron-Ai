# Deployment Foundation

Docker is a future development/server-infrastructure option, not a desktop
installation requirement. No database, Redis, model runtime, or cloud service
is started by Step 01. CI validates the foundation on Linux; desktop packaging
and OS matrix validation begin with the Tauri shell milestone.
