# Infrastructure Boundaries

`docker/`, `ci/`, `deployment/`, and `monitoring/` are reserved for tracked
infrastructure artifacts as they become real. Step 01 uses GitHub Actions for
CI and does not require Docker for local development or desktop users.
