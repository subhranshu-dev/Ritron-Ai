# Desktop Workspace Reservation

The desktop shell is intentionally not implemented in Step 01. This directory
is reserved for the Step 12 Tauri 2, Rust, React, and TypeScript application.

Future desktop code must keep provider credentials and other secrets outside
frontend bundles, communicate with the local core through documented adapters,
and isolate platform-specific capabilities behind native layers.
